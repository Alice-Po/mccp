---
import Layout from '../layouts/Layout.astro';
import DonutChart from '../components/DonutChart.svelte';
import { aggregateData, type BudgetItem } from '../utils/budget-data';

// Charger les données budgétaires
console.log('🔄 Finances - Chargement des données...');

// Essayer d'abord un import direct du fichier
let budgetData: BudgetItem[] = [];
try {
  // Import direct depuis le public folder
  budgetData = await import('../../public/assets/datas/2025/base_budget_2025.json').then(module => module.default as BudgetItem[]);
  console.log('📊 Finances - Import direct réussi:', budgetData?.length, 'éléments');
} catch (importError) {
  console.log('⚠️ Finances - Import direct échoué, essai fetch:', importError);
  
  // Fallback avec fetch
  try {
    const response = await fetch('/assets/datas/2025/base_budget_2025.json');
    console.log('📡 Finances - Réponse fetch:', response.status, response.ok);
    budgetData = await response.json();
    console.log('📊 Finances - Fetch réussi:', budgetData?.length, 'éléments');
  } catch (fetchError) {
    console.error('❌ Finances - Fetch échoué:', fetchError);
    budgetData = [];
  }
}

console.log('📈 Finances - Données budgétaires finales:', budgetData?.length, 'éléments');

// Préparer les données pour les deux onglets : Dépenses et Recettes
console.log('🔧 Finances - Agrégation des données...');

const depensesData = aggregateData(budgetData, 'FONCTIONNEMENT', 'DEPENSES', 'regroupement_focale_n1');
const recettesData = aggregateData(budgetData, 'FONCTIONNEMENT', 'RECETTES', 'regroupement_focale_n1');

console.log('📊 Finances - Données dépenses:', depensesData?.length, 'groupes');
console.log('📊 Finances - Données recettes:', recettesData?.length, 'groupes');

// Données par défaut (dépenses)
const initialData = depensesData;
---

<Layout title="Finances - MCCP">
  <main class="finances-main">
    <div class="finances-container">
      <!-- En-tête -->
      <header class="finances-header">
        <h1>Finances municipales</h1>
        <p>Visualisation des données budgétaires 2025</p>
      </header>

      <!-- Section graphique avec onglets -->
      <section class="chart-section">
        <!-- Onglets -->
        <div class="tabs-container">
          <div class="tabs">
            <button class="tab tab-active" data-tab="depenses">
              💰 Dépenses
            </button>
            <button class="tab" data-tab="recettes">
              💵 Recettes
            </button>
          </div>
        </div>

        <!-- Graphique DonutChart -->
        <div class="chart-wrapper">
          <DonutChart 
            data={initialData}
            title="Dépenses - Réalisations 2024"
            client:load
          />
        </div>
      </section>

      <!-- Script pour gérer les onglets -->
      <script define:vars={{ depensesData, recettesData }}>
        document.addEventListener('DOMContentLoaded', () => {
          console.log('🎛️ Finances - Initialisation onglets');
          console.log('📊 depensesData:', depensesData);
          console.log('📊 recettesData:', recettesData);

          const tabs = document.querySelectorAll('.tab');
          const chartWrapper = document.querySelector('.chart-wrapper');
          
          let currentData = depensesData;
          let currentTitle = "Dépenses - Réalisations 2024";

          function updateChart(data, title) {
            console.log('🔄 Finances - Mise à jour chart:', { data: data?.length, title });
            
            // Dispatch un événement personnalisé pour mettre à jour le composant Svelte
            const updateEvent = new CustomEvent('updateChart', {
              detail: { data, title }
            });
            document.dispatchEvent(updateEvent);
          }

          tabs.forEach(tab => {
            tab.addEventListener('click', () => {
              console.log('🎯 Finances - Clic onglet:', tab.dataset.tab);

              // Mettre à jour les styles des onglets
              tabs.forEach(t => t.classList.remove('tab-active'));
              tab.classList.add('tab-active');

              // Déterminer les nouvelles données
              if (tab.dataset.tab === 'depenses') {
                currentData = depensesData;
                currentTitle = "Dépenses - Réalisations 2024";
              } else {
                currentData = recettesData;
                currentTitle = "Recettes - Réalisations 2024";
              }

              // Mettre à jour le graphique
              updateChart(currentData, currentTitle);
            });
          });
        });
      </script>
    </div>
  </main>
</Layout>

<style>
  .finances-main {
    min-height: 100vh;
    padding: 2rem 0;
    background-color: #f9fafb;
  }

  .finances-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .finances-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .finances-header h1 {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--secondary);
    margin-bottom: 1rem;
  }

  .finances-header p {
    font-size: 1.1rem;
    color: #6b7280;
    max-width: 600px;
    margin: 0 auto;
  }

  .chart-section {
    background-color: white;
    border-radius: 1rem;
    padding: 2rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    margin-bottom: 2rem;
  }

  .tabs-container {
    margin-bottom: 2rem;
  }

  .tabs {
    display: flex;
    gap: 0.5rem;
    border-bottom: 2px solid #f1f5f9;
    margin-bottom: 1rem;
  }

  .tab {
    background: none;
    border: none;
    padding: 0.75rem 1.5rem;
    font-size: 1rem;
    font-weight: 500;
    color: #64748b;
    cursor: pointer;
    border-bottom: 3px solid transparent;
    transition: all 0.2s ease;
    position: relative;
  }

  .tab:hover {
    color: var(--primary);
    background-color: #f8fafc;
  }

  .tab-active {
    color: var(--primary) !important;
    border-bottom-color: var(--primary) !important;
    background-color: #f8fafc;
  }

  .chart-wrapper {
    min-height: 400px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .finances-main {
      padding: 1rem 0;
    }

    .finances-header h1 {
      font-size: 2rem;
    }

    .finances-header p {
      font-size: 1rem;
    }

    .chart-section {
      padding: 1.5rem;
    }

    .tabs-container {
      margin-bottom: 1.5rem;
    }

    .tab {
      padding: 0.625rem 1rem;
      font-size: 0.9rem;
    }

    .chart-wrapper {
      min-height: 300px;
    }
  }
</style>